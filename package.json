{
  "name": "job-tracker-extension",
  "version": "1.0.0",
  "description": "Chrome extension to automatically track job applications from Gmail using AI",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:prod": "node build.js --production",
    "dev": "node build.js --watch",
    "clean": "rm -rf dist",
    "package": "npm run build:prod && rm -f jobtracker.zip && zip -r jobtracker.zip manifest.json assets/icon16.png assets/icon32.png assets/icon48.png assets/icon128.png src/*.html src/*.css dist/*.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0"
  }
}
